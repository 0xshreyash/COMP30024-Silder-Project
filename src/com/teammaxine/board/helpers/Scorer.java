package com.teammaxine.board.helpers;

import com.teammaxine.board.elements.Board;

/**
 * Created by noxm on 17/04/17.
 */
public class Scorer {
    public static double scoreBoard(Board board, String player) {
        double score = 0;
        if(player.equals("H")) {
            // score board for h
        } else {
            // score board for v
        }
        return score;
    }
}
